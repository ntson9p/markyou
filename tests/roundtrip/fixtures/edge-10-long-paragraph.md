This is a deliberately long single paragraph that keeps going with many words to exercise serialization of long runs of text without any line-wrapping configuration, including *inline emphasis*, **strong text**, `inline code`, a [link](https://example.com/very/long/path/that/keeps/going?with=query&params=true), some math $f(x) = x^2$, and ~~struck words~~, all mixed together in one continuous flow that must survive byte-for-byte once normalized, no matter how long it grows or how many inline constructs it contains along the way.
