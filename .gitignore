node_modules/
dist/
dev-dist/
coverage/
playwright-report/
test-results/
temp/
.firebase/
*.local
.DS_Store
*.tsbuildinfo

# Never commit credentials. Nothing in this project needs them today, but a
# public repo is the wrong place to find out that something started to.
.env
.env.*
!.env.example
*.pem
*.key
